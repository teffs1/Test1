name: test1

on:
    push:
        branches: [ $default-branch ]
    pull_request:
        branches: [ $default-branch ]

jobs:
  job:
    runs-on: ubuntu-latest
    steps: 
      - name: Setup Python
        uses: actions/setup-python@v5.5.0
                    

  
  
