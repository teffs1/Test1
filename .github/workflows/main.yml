name: test1

on:
    push:
        branches: main
    pull_request:
        branches: main

jobs:
  job:
    runs-on: ubuntu-latest
    steps: 
      - name: Setup Python
        uses: actions/setup-python@v5.5.0
                    

  
  
